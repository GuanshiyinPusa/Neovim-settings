-- Trouble: a wrapping, navigable panel for diagnostics / references / symbols.
-- Complements tiny-inline-diagnostic (inline) with a full project-wide list.
-- Keymaps (<leader>x*) live in lua/keymap.lua.
return {
	"folke/trouble.nvim",
	cmd = "Trouble",
	opts = {},
}
